# Maintainer: s8321414 <s8321414 at gmail.com>
# Arch Linux maintainers and contributors:
# Maintainer: Zatherz <zatherz at linux dot pl>

pkgname=qt5ct
pkgver=0.9
pkgrel=1
pkgdesc="Qt5 Configuration Utility (requires Qt platform theme to be set to qt5ct)"
arch=('x86_64')
license=('BSD')
url="http://qt-apps.org/content/show.php/Qt5+Configuration+Tool?content=168066"
depends=("qt5-base" "qt5-svg")
makedepends=("qt5-tools")
sha256sums=('14eff36bb4425d72ea375d7f3f584ecbdfb7f705f76013961d916cc01137b392')
source=("http://qt-apps.org/CONTENT/content-files/168066-${pkgname}-${pkgver}.tar.bz2")
install=${pkgname}.install

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  qmake-qt5 PREFIX=/usr
  make
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make INSTALL_ROOT="${pkgdir}" install
}
